baseURL = "/"
languageCode = "en-us"
title = "AtlasShelf"

[params]
  description = "Static catalog generator with faceted browsing."

[taxonomies]
  tag = "tags"
  category = "categories"

[outputs]
  home = ["HTML", "JSON"]
  section = ["HTML", "JSON"]

[outputFormats.JSON]
  mediaType = "application/json"
  baseName = "items-index"
  isPlainText = true
