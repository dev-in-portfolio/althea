# Althea

SpecForge — Eleventy spec compiler for YAML-driven documentation.

## Run locally

1. `npm install`
2. `npm run build`
3. `npm run dev`

Specs live in `src/specs/`. Search index outputs to `/_spec_index.json`.
