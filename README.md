# Althea

FormFoundry — schema-driven form builder with public publishing and response inbox.

## Run locally

1. `npm install`
2. `export DATABASE_URL=postgresql://...`
3. `npm run dev`

App runs at `http://127.0.0.1:3020`.
