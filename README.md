# Althea

Chronicle — timeline authoring tool with layered events and zoomable navigation.

## Run locally

1. `npm install`
2. `export DATABASE_URL=postgresql://...`
3. `npm run dev`

App runs at `http://127.0.0.1:3021`.
